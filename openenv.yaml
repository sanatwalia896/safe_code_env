spec_version: 1
name: safe_code_env
description: "Safe Code Env: a workspace-based coding environment where agents inspect files, edit code, run tests, and submit final filesystem state for grading."
type: space
runtime: fastapi
app: server.app:app
port: 8000
tags:
  - openenv
  - coding
  - safety
  - filesystem
  - tools
tasks:
  - id: task_1
    difficulty: easy
    description: "Shared codebase task: fix FastAPI /health response contract and SQLite readiness signal"
  - id: task_2
    difficulty: medium
    description: "Shared codebase task: remove SQLite SQL injection risk in repository query logic"
  - id: task_3
    difficulty: medium
    description: "Shared codebase task: enforce protected-path policy for .env and production database files"
  - id: task_4
    difficulty: medium
    description: "Shared codebase task: enforce safe local git command policy (no reset/restore/destructive flows)"
